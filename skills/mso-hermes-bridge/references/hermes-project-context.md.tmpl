# Hermes Agent ― MSO Project Context
# Auto-generated by mso-hermes-bridge setup. Do not edit manually.
# Source: mso-hermes-bridge/references/hermes-project-context.md.tmpl

## 이 저장소에 대하여

이 저장소는 **MSO (Multi-Swarm Orchestrator)** v{{MSO_VERSION}}으로 관리되는 프로젝트다.
프로젝트 루트: `{{PROJECT_ROOT}}`
초기화 날짜: {{INIT_DATE}}

## MSO 구조

```
agent-context/
├── index/index.yaml          # artifact registry (SSOT)
├── workflow/*.abox.ttl       # workflow topology (SSOT)
├── observability/            # 파생 관측 리포트
└── work-memory/              # 작업 기억 (jsonl)
```

## Hermes 역할

이 저장소에서 Hermes는 **MSO Executor** 로 동작한다.

- 역할: workflow TTL에서 `delegates_to: hermes-executor`로 선언된 step을 실행한다.
- ExecutionMethod: api (HTTP POST via mso-hermes-bridge)
- ExecutionSubject: Agent (Hermes)

## 실행 원칙

1. **Artifact First**: 입력은 workflow의 `consumes` artifact에서 읽고, 출력은 `produces_to` artifact 경로에 저장한다.
2. **TTL 수정 금지**: workflow TTL ABox는 직접 수정하지 않는다. 결과 artifact만 생성한다.
3. **work-memory 기록**: 중요 결정이나 이슈는 `agent-context/work-memory/`에 기록한다.
4. **구조화 출력**: 응답은 Markdown 또는 JSON으로 구조화해 반환한다 ― 단순 텍스트 스트림은 MSO artifact로 저장하기 어렵다.

## 참조 파일

- workflow topology: `agent-context/workflow/*.abox.ttl`
- artifact registry: `agent-context/index/index.yaml`
- 현재 작업 메모리: `agent-context/work-memory/auditlog/`
- MSO bridge: `skills/mso-hermes-bridge/scripts/`

## 금지사항

- `agent-context/workflow/*.abox.ttl` 직접 편집
- `agent-context/index/index.yaml` 임의 수정
- work-memory 없이 중요 결정 실행
