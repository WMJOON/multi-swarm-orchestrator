# module.improvement-proposal

## 목적
관찰된 이상 신호를 설계 개선 제안으로 구조화.

- 제안은 `target_skills`(예: `mso-workflow-topology-design`, `mso-mental-model-design`)를 포함해야 한다.
- `rollback_rule`가 없는 제안은 `event_type=anomaly_detected` 경고만 남기고 종료한다.
